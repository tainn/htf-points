# htf-points

Award points in the `hold-the-flag` mode

- increment `playerWin` to a player upon them capturing their own team flag
- increment `teamWin` instead of `teamLoss` to a team upon them having their flag captured
